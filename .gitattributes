# LF in the repository and LF in the working tree, on every platform.
# Swift source, the manifest and the workflow files are all compared byte for
# byte somewhere in CI, and a checkout that rewrites line endings fails those
# comparisons for a reason that is invisible in the diff.
* text=auto eol=lf
*.png binary
*.xcframework binary
